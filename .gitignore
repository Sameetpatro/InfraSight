# OS
.DS_Store
Thumbs.db

# Environment & secrets
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/
*.egg
pip-wheel-metadata/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.cover
.coverage
htmlcov/

# Virtual environments (anywhere in repo)
.venv/
venv/
ENV/
env/

# Hugging Face / local ML cache
.hf_cache/

# Misc Python temp
.pyc_tmp/

# Backend — local DB & generated marking PNGs
backend/data/*.db
backend/data/results/
backend/.venv/
backend/venv/

# Large weight blobs under backend (not PixelMapINT — that checkpoint ships with this project)
backend/*.pt

# Node / React
node_modules/
frontend/node_modules/
frontend/build/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp.*
.yarn/

# IDE (optional — remove lines if you want these committed)
.idea/
.vscode/
*.swp
*.swo

# Local ML weights & PixelMapINT artifacts (keep on disk, not in git)
model/
